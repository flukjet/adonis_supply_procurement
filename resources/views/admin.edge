@layout('layouts/masteradmin')

@set('title','admin')

@section('body')
    <div class="ui segment">
        <div>
            <h3>Order lists</h3>
            <div class="ui dropdown" style="position: absolute; right: 10px; top:15px;">
                <div class="text">Product Management</div>
                <i class="dropdown icon"></i>
                <div class="menu">
                    <div class="item">
                        <a class="" href="" style = "color:black;">Add Product</a>
                    </div>
                    <div class="item">
                        <a class="" href="" style = "color:black;">Update Product</a>
                    </div>
                    <div class="item">
                        <a class="" href="{{route('delProductPage')}}" style = "color:black;">Delete Product</a>
                    </div>
                </div>
            </div>
        </div>


        <table class="ui celled table">
            <tr class="center aligned">
                <th>id</th>
                <th>name</th>
                <th>product(quantity)</th>
                <th>note</th>
                <th>status</th>
                <th></th>
            </tr>
        @each(ticket in tickets)
            <tr class="center aligned">
                <td>{{ticket.id}}</td>
                <td>{{ticket.user.name}}</td>
                <td>ปากกาน้ำเงิน(3)</td>
                <td>{{ticket.note}}</td>
                <td>{{ticket.ticketstatus.name}}</td>
                <td>
                    <a class="ui green mini button" href="{{route('admin.approve',{id: ticket.id})}}">Approve</a>
                    <a class="ui red mini button" href="{{route('admin.reject',{id: ticket.id})}}">Reject</a>
                </td>
            </tr>
        @endeach
        </table>
    </div>

@endsection




